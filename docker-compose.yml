services:
  web:
    build:
      context: .
    ports:
      - "3000:3000"
    environment:
      NODE_ENV: production
      PORT: 3000
      PARROT_DB_PATH: /data/database.sqlite
      PARROT_HOSTS: "${PARROT_HOSTS:-}"
    volumes:
      - parrot-db:/data
    restart: unless-stopped

  friendtrack:
    build:
      context: ./friendtrack
    ports:
      - "8000:8000"
    environment:
      PORT: 8000
      FRIENDTRACK_DATA_DIR: /app/data
    volumes:
      - "${FRIENDTRACK_DATA_PATH:-./friendtrack/data}:/app/data"
    restart: unless-stopped

volumes:
  parrot-db:
